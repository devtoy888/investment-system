# Cron Job: hy3-free-expiry-switchback

**Job ID:** bf09885148a6
**Run Time:** 2026-07-20 16:01:14
**Schedule:** once at 2026-07-21 00:00

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

今天（7月21日）Tencent Hy3 免费模型已到期，需要将 Hermes Agent 的主模型和子代理模型切回 DeepSeek V4。

请执行以下步骤：
1. 主模型切回 flash：
   hermes config set model.default deepseek-v4-flash
   hermes config set model.provider deepseek
   hermes config set model.base_url https://api.deepseek.com
2. 子代理（delegation）切回 pro：
   hermes config set delegation.model deepseek-v4-pro
   hermes config set delegation.provider deepseek
3. 验证：grep -A3 '^model:' 和 grep -A2 '^delegation:' 确认都改回
4. 通知用户：已自动切回 DeepSeek V4（主模型=deepseek-v4-flash，子代理=deepseek-v4-pro），建议 /reset 或重启 Hermes 生效

注意：HERMES_HOME 是 /opt/data/profiles/llm-wiki，hermes 二进制在 /opt/hermes/bin/hermes。

## Response

验证通过，配置已成功切回。

---

**✅ DeepSeek V4 切换完成（Hermes profile: llm-wiki）**

配置文件：`/opt/data/profiles/llm-wiki/config.yaml`

**主模型（model）**
- `default`: `deepseek-v4-flash`
- `provider`: `deepseek`
- `base_url`: `https://api.deepseek.com`

**子代理（delegation）**
- `model`: `deepseek-v4-pro`
- `provider`: `deepseek`

5 条 `hermes config set` 全部执行成功，`grep` 复核结果与预期一致，已取代原先到期的 `tencent/hy3:free`（openrouter）。

**⚠️ 生效方式**：配置为启动期读取，需 **`/reset` 或重启 Hermes** 后新会话才会使用 DeepSeek V4。当前正在运行的会话仍沿用旧模型。

**备注（未改动，供参考）**：`fallback_providers['3']` 仍指向 `openrouter` 的 `deepseek/deepseek-v4-flash` 作为兜底；如需让兜底链路也走直连 `deepseek` provider，可另行告知，我再调整。

建议下一步：`/reset` 或重启 Hermes Agent 使配置生效。
