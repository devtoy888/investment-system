✅ 完成 — 基金系统自进化机制 实施总结

## 维度A: 数据合理性校验 + 推送异常标注

### fund_tools.py
- 新增 `run_sanity_checks(raw_data)` ✅
  - 大盘指数价格范围（上证2500-4500, 科创50 800-3000等）
  - 板块采集率（>=50%）
  - 涨跌家数合计（>=100）
  - 两市成交额范围（500-50000亿）
  - 北向资金范围（-200~+200亿）
  - 基金采集率（>=60%）
  - KOL采集完整性
  - 返回 {status, checks[], warnings[], issue_count}

### 三条预采集脚本
- collect_morning_data.py → 写入 `_sanity_report.json` ✅
- collect_noon_data.py → 写入 `_noon_sanity.json` ✅
- closing_review.py → 写入 `_closing_sanity.json` ✅

### 三条 cron prompt
- 今日参考: 追加 `cat /tmp/fund_data/_sanity_report.json` + 异常时推送头加⚠️ ✅
- 盘中速递: 追加 `cat /tmp/fund_data/_noon_sanity.json` + 同样 ✅
- 收盘复盘: 追加 `cat /tmp/fund_data/_closing_sanity.json` + 同样 ✅

## 维度B: 信号归因追踪

### fund_tools.py
- `extract_signals_from_kols(kol_posts, push_type, quotes)` ✅
  - 从KOL博文中匹配SIGNALS词典中的信号词
  - 区分 bullish/bearish/neutral 方向
  - 映射到对应指数（科技→科创50，大盘→上证指数等）
  - 记录当日价格作为基准点
- `store_signals(signals)` → 写入 `signals.jsonl` ✅
- `resolve_past_signals(quotes)` → 查询3-7天前未解析信号，比对实际涨跌 ✅
  - 写入 `signals-resolved.jsonl`
- `generate_signal_report()` → 生成近30天KOL信号准确率排行榜 ✅
  - 自动分级: ✅>=70% / 🔶>=50% / ❌<50%

### 预采集脚本集成
- morning/noon: 提取信号+存储 ✅
- closing: 解析历史信号 ✅

## 下周一到周三首次产出
- 第一轮信号存储：明天（7/1）早盘
- 第一轮信号解析：7/4（需等3天数据积累）
- 第一份信号准确率报告：7月中旬（需积累30天数据）
